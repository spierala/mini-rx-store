import { Routes } from '@angular/router';
import { TodosSimpleShellComponent } from './components/todos-simple-shell/todos-simple-shell.component';

export const routes: Routes = [{ path: '', component: TodosSimpleShellComponent }];
