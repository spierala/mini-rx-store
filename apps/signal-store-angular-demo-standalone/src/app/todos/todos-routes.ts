import { Routes } from '@angular/router';
import { TodosShellComponent } from './components/todos-shell/todos-shell.component';

export const routes: Routes = [{ path: '', component: TodosShellComponent }];
